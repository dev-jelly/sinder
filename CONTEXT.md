# Sinder

목표: macOS의 차분하고 유려한 디자인과 Windows 탐색기의 실용적인 파일 작업을 결합한다. 로컬과 SSH 원격을 동일한 탐색·선택·전송 모델로 사용한다.

## 제품 범위

- 실제 데스크톱 앱. macOS 우선, Electron으로 Windows/Linux 확장 가능.
- 로컬 및 SSH/SFTP 위치, 연결 프로필, SSH agent/키/비밀번호 인증, 호스트 키 검증.
- 독립 창, 사이드바, 즐겨찾기, 탭, 분할 보기, 경로 입력, 이동 기록, 목록/아이콘 보기, 정렬, 폴더 내 검색, 숨김 파일.
- 다중 선택, 복사/잘라내기/붙여넣기, 드래그 전송, 새 폴더, 이름 변경, 휴지통, 미리보기.
- 로컬↔원격 및 원격↔원격 전송, 진행/취소/오류, 이름 충돌 보호.
- 연결 오류와 재연결, 실제 파일로 통합 검증, 실행 가능한 macOS 앱 산출물.

## 용어와 구조

- Connection: 로컬 장치 또는 저장된 SSH 프로필의 현재 연결 상태.
- Location: connectionId + 절대 경로. 모든 탐색/파일 작업은 이 값으로 표현.
- Window: 독립적인 탐색·선택·탭을 가지며 연결, 전송 큐, 편집 세션과 파일 클립보드는 프로세스에서 공유한다. 첫 창의 레이아웃을 다음 실행에 복원하고 추가 창은 현재 위치에서 시작한다.
- External export: 외부 파일 관리자에 전달할 사본. macOS 원격 드래그는 NSFilePromiseProvider로 Finder가 놓기를 수락한 뒤 다운로드하고 완성된 사본을 전달한다. 서버 원본을 삭제하지 않는다. 다른 OS에서는 먼저 사본을 준비하는 경로를 유지한다.
- Pane: 독립적인 선택·이동 기록을 갖는 탐색 영역. 탭마다 최대 두 개.
- Transfer: 출발 위치 목록과 목적 폴더를 갖는 순차 작업. 복사 완료 후 이동 원본을 제거.
- LocalProvider / SftpProvider: 파일 시스템 차이를 흡수하는 공통 인터페이스.
- EditSession: 서버의 일반 파일과 이 기기의 편집용 캐시 사이 관계. 서버·계정·키 식별자와 내용 해시를 기준으로 자동 반영하며, 충돌·연결 단절·앱 재시작 때 캐시와 복구 기록을 유지한다.

원격 편집은 사용자 지정 외부 편집기(macOS 기본 TextEdit)에서 저장하면 자동 반영한다. 전송의 충돌 정책과 별개이며, 변경된 서버본은 내용으로 검출하고 명시적 수정본 적용 전에는 교체하지 않는다. 저장 전 서버본은 숨김 백업 폴더에 보존한다. 중단된 두 단계 이름 변경은 write-ahead 기록으로 복구한다. 같은 프로필 ID라도 서버나 계정이 바뀌면 과거 편집본을 보내지 않는다.

SSH 파일 작업은 SFTP로 수행하며 셸 명령 문자열을 만들지 않는다. renderer는 격리하고 제한된 IPC만 노출한다. 비밀번호·키 암호는 저장하지 않는다. 처음 보는 서버 키는 사용자가 확인하며 변경된 키는 연결을 차단한다.

## 디자인

시스템 글꼴, 쿨 그레이 표면, 파란색 선택 강조, 반투명 사이드바, 조밀하되 읽기 쉬운 파일 행. 장식보다 경로/선택/연결 상태를 우선한다. 다크 모드와 reduced motion을 따른다.
